Options -Indexes
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Elze.eg/

    # If accessing the root or a non-public path, redirect to public/
    RewriteCond %{REQUEST_URI} !^/Elze\.eg/public/
    RewriteRule ^(.*)$ public/$1 [L,NC]
</IfModule>
